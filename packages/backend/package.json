{
  "name": "@hexis/backend",
  "scripts": {
    "dev": "wrangler dev --config wrangler.dev.jsonc",
    "deploy": "wrangler deploy --minify",
    "deploy:dev": "wrangler deploy --minify --config wrangler.dev.jsonc",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
